import mongoose from 'mongoose';

const Schema = mongoose Schema;

const OrderSchema = new Schema ({
    AdressFrom:{type: String},
    AdressName:{type: String},
    Name:{type: String},
    Phone:{type: Number},
    Vechile:{type:String},
    Discription:{type:String}
});

const Order=mongoose model('Order',OrderSchema);